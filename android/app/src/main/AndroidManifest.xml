<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET is used only for optional, LAN-hosted media downloads. Core
         lessons remain bundled and playable without a network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- No REQUEST_INSTALL_PACKAGES: the app never self-installs APKs. Updates
         are delivered out-of-band (e.g. adb/manual install) rather than from a
         LAN host, so a child device holds no package-install privilege. -->
    <!-- No USE_BIOMETRIC: biometric auth is not implemented (audit #13);
         re-add the permission together with BiometricPrompt when shipped. -->

    <application
        android:name=".MaxinesWorldApp"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaxinesWorld">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
