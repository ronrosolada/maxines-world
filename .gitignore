# ─── Phase 7 hygiene: untracked artifacts (kept on disk, never committed) ───
# Hermes Desktop attachments (design handoffs, zips, uploads) — not project assets
.hermes/

# Dev emulator screenshots — transient evidence, not deliverables
emulator-*.png
baseline-before.png

# Build / IDE
.gradle/
build/
local.properties
.idea/
*.iml
.DS_Store

__pycache__/
*.pyc
android/.kotlin

# Content engine voice models (121MB, download via tools/content_engine/README.md)
voice_models/

# Local quickbits cache
quickbits/
